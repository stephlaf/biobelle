<%= render 'shared/banner' %>

<div id="products-index", class="container text-center">
  <h1>Nos Produits</h1>
  <div id="index" class="products">
    <div class="row" id="js-products">
      <% @products.order(name: :asc).each do |product| %>
      <%# binding.pry %>
        <%= render partial: 'shared/product_card', locals: {product: product} %>
      <% end %>    
    </div>
  </div>
</div>
